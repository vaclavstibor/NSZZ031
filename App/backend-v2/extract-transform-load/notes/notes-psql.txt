- connect to articles_db: psql -U postgres -d articles
- tables: \dt

-- ted potrebuju na zacatku etl odstranit vsechno z tables
-- mozna problem s tickery, nemam osetreno kdyz jich je vice v articlech

"""
articles where is AAPL (get only titles)

SELECT a.id, a.title, a.published_date
FROM articles a
JOIN article_tickers at ON a.id = at.article_id
JOIN tickers t ON at.ticker_id = t.id
WHERE t.name = 'AAPL';
"""